# Gitコマンド
いつもわからなくなって調べるコマンドたち

git addを取り消す
```title="全ファイル"
git rm --cached -r .
```
```title="ファイル指定"
git rm --cached -r file_name
```

git commitを取り消す
```
git reset [commit-ID]
```

## Reference
[git](https://git-scm.com/docs)
